title: InvestEd
description: Investment education platform for Cambridge ACE students — learn, practice, and grow your financial literacy.

url: "https://manasvigali.github.io"
# Local dev: baseurl is empty. For production use: bundle exec jekyll build --baseurl "/communityproject"
baseurl: ""

# Exclude build files
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
