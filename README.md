# InvestEd (Jekyll)

This is a Jekyll version of the InvestEd site. It uses jekyll-theme-slate with a custom layout that includes your CSS/JS under `assets/`.

Local build:
  bundle install
  bundle exec jekyll serve --baseurl "/communityproject"
